
export default function LoginLayout({ children }) {
  return (
    <div style={{
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      height: "100vh",
      backgroundColor: "#dfe6e9",
    
    }}>
      {children}
    </div>
  );
}
